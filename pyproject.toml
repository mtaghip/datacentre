[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "datacentre-mvp"
version = "0.1.0"
description = "Auditable MVP calculation core for AI-assisted data centre concept design."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Datacentre" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
